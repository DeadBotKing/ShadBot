# بررسی اجرای دوم — بعد از سه اصلاح

مخزن: `DeadBotKing/ShadBot` @ `7bef767` («Agent Make Code 2») · ۲۰۲۶-۰۸-۱۵

---

## حکم کوتاه

**سه اصلاح کار کردند. اما کد تولیدشده هنوز آنچه خواسته شده نیست — و یک باگ جدید پیدا کردم.**

گیت حالا `GREEN` می‌دهد روی کدی که با `TypeError` می‌ترکد.

---

## ۱. آنچه واقعاً بهتر شد

| معیار | اجرای ۱ | اجرای ۲ |
|---|---|---|
| هدف گیت | پلتفرم (۱۰۹۴ فایل) | **پروژه (۶ فایل)** ✅ |
| فایل‌های یکتا | ۶ از ۱۴ | **۶ از ۶** ✅ |
| ماژول‌های قابل import | **۰ از ۱۴** | **۳ از ۳** ✅ |
| ماژول‌های چسبیده | ۷۰ سرآیند | **۰** ✅ |
| `__init__.py` | ۰ | **۳** ✅ |
| `[PASS] pytest` دروغین | بله | **`[SKIP]` صادقانه** ✅ |

خط `[QUALITY GATE TARGET]` مسیر درست را نشان می‌دهد، `[CODE GENERATION] Split
response into 5 module(s)` در هر ۱۱ فراخوانی کار کرد، و `mypy` که قبلاً روی
۱۰۹۴ فایل پلتفرم FAIL می‌داد حالا روی کد واقعی `Success: no issues found in 6
source files` می‌دهد.

**اما این پیشرفت در سنجش است، نه در محصول.**

---

## ۲. باگ L — گیت GREEN می‌دهد روی کدی که کرش می‌کند (جدید، بحرانی)

کد تولیدشده import می‌شود، پس چک جدید من `[PASS] imports` می‌دهد. ولی وقتی
**اجرا** شود:

```
$ cd src && python -m agentplatform

Traceback (most recent call last):
  File "src/agentplatform/__main__.py", line 12, in main
    result = app_service.process_data('input_data')
  File "src/agentplatform/application/stateless_application_service.py", line 6
    return self.domain_layer.data(data)
           ~~~~~~~~~~~~~~~~~~~~~~^^^^^^
TypeError: 'dict' object is not callable
```

باگ در دو خط تولیدشده است:

```python
# domain/immutable_domain.py
@property
def data(self):          # ← property، یعنی مقدار است
    return self._data

# application/stateless_application_service.py
def process_data(self, data):
    return self.domain_layer.data(data)   # ← ولی مثل تابع صدا زده می‌شود
```

و حکم گیت روی همین کد:

```
[DETERMINISTIC GATE] Status: GREEN (ALL EXECUTED CHECKS PASSED) | executed=4/7
  [PASS] syntax    | [PASS] imports | [PASS] security | [PASS] architecture
```

**چرا از چک import رد شد:** import کردن یک ماژول فقط بدنه‌ی سطح ماژول را اجرا
می‌کند. `main()` هرگز صدا زده نمی‌شود، پس باگ دیده نمی‌شود. چک import مرز
«کد وجود دارد» را می‌سنجد، نه «کد کار می‌کند» — و من در گزارش قبلی همین را
نوشتم. حالا معلوم شد آن مرز کافی نیست.

**چیزی که لازم است:** یک چک smoke-run که نقطه‌ی ورود (`run.py` / `__main__.py`)
را در زیرفرایند واقعاً اجرا کند و کد خروج غیرصفر را FAIL بدهد. این تنها چیزی
است که این کلاس از باگ را می‌گیرد.

ضمناً `run.py` هنوز همان قالب ثابت است که «operational» چاپ می‌کند بدون اینکه
چیزی import کند — برای همین `python run.py` موفق به‌نظر می‌رسد در حالی که
`python -m agentplatform` می‌ترکد.

---

## ۳. علت ریشه‌ای پاسخ‌های تکراری — پیدا شد

در لاگ شما `1746 chars` **هشت بار** تکرار شد. علتش را دقیقاً پیدا کردم.

معمار ۱۱ فایل با هدف‌های کاملاً متفاوت درخواست می‌کند
(`architect_agent.py` خط ۱۶۸–۱۸۰):

```
domain/agents/agent_role.py              → Agent identity and role enumeration (Phase 2)
application/release/release_service.py   → Production Freeze V1.0 & SLA governance (Phase 12)
...
```

ولی `PromptBuilder.build()` این هدف را فقط در فیلد `Instructions` می‌گذارد و
کنارش `task_description` کامل، `metadata`، `intelligence_context` و
`memory_context` را می‌ریزد. نتیجه را اندازه گرفتم:

```
prompt A (agent_role.py)      : 1676 chars
prompt B (release_service.py) : 1692 chars
SIMILARITY                    : 96.4%
```

**تنها تفاوت یک خط است** که وسط دیوار متن دفن شده. مدل عملاً یازده بار یک
پرامپت می‌بیند، پس یازده بار یک جواب می‌دهد — و چون حالا splitter مسیرها را
از خودِ پاسخ می‌خواند، هر ۱۱ بار روی همان ۵ فایل نوشته شد. آخرین پاسخ برنده
شد و بقیه دور ریخته شدند.

این توضیح می‌دهد چرا **۶۵۹ ثانیه** طول کشید تا ۳۲ خط کد تولید شود.

---

## ۴. آنچه خواسته شد در برابر آنچه ساخته شد

از `tasks/backlog.yaml`:

> Implement the complete ShadBot Agent Platform from Phase 1 through Phase 12.
> 1. Domain Layer: **immutable** domain contracts and entities
> 2. Application Layer: stateless application services
> 3. Quality Gate: 100% GREEN

| خواسته | نتیجه |
|---|---|
| فاز ۱ تا ۱۲ | **۰ از ۱۲** — نه orchestration، نه quality_gate، نه release |
| دامنه‌ی immutable | **۰** `@dataclass(frozen=True)` — یک کلاس ساده با `_data` |
| سرویس بدون حالت | یک کلاس با یک متد که کرش می‌کند |
| PEP 484 (در پرامپت صریح) | **۰ از ۵** تابع annotation دارد |
| docstring (در پرامپت صریح) | **۱** فایل (آن هم `"""Package marker generated by ShadBot."""` که خودِ کد من نوشته) |
| تست | **۰** |

کل خروجی ۳۲ خط است. اجرای قبلی ۷۴۲ خط بود — ولی آن ۷۴۲ خط ۷ فایل تکراری و
غیرقابل‌اجرا بود. حالا کد کمتر ولی منسجم است. **هیچ‌کدام نزدیک خواسته نیست.**

---

## ۵. قدم بعدی — به‌ترتیب اهمیت

**۱. پرامپت per-file (بزرگ‌ترین برد).** پرامپت تولید کد باید فقط هدف همان فایل
را داشته باشد، نه کل `task_description`. الان ۹۶.۴٪ شباهت است؛ باید زیر ۳۰٪
برسد. این هم کیفیت را بالا می‌برد و هم آن ۶۵۹ ثانیه را کوتاه می‌کند.

**۲. چک smoke-run در گیت.** نقطه‌ی ورود را واقعاً اجرا کند. بدون این، گیت
همچنان روی کد کرش‌کننده GREEN می‌دهد.

**۳. کش روی هش پرامپت.** وقتی ۱۱ فراخوانی یک پرامپت دارند، ۱۰ تای‌شان اتلاف
محض است.

**۴. `run.py` واقعی.** باید نقطه‌ی ورود را import و اجرا کند تا شکست را نشان
دهد، نه «operational» چاپ کند.

---

## ۶. جمع‌بندی صادقانه

سه اصلاح قبلی دقیقاً کاری را کردند که قرار بود: **خرابی را قابل دیدن کردند.**
قبلاً گیت درباره‌ی پلتفرم حرف می‌زد و کد ایجنت اصلاً قابل import نبود؛ حالا گیت
درباره‌ی کد ایجنت حرف می‌زند و آن کد بارگذاری می‌شود.

ولی «قابل دیدن» با «درست» فاصله دارد. الان بزرگ‌ترین مانع لایه‌ی prompt است، و
گیت هنوز یک سوراخ دارد (باگ L) که با smoke-run بسته می‌شود.

---
---

# پیوست — چهار اصلاح انجام شد

سوئیت: **۲۵۴ passed + ۱ skipped** (از ۲۳۹، با ۱۵ تست جدید)

---

## اصلاح ۱ — پرامپت per-file (باگ M)

**فایل‌ها:** `application/prompt/prompt_builder.py` (بازنویسی),
`application/generation/code_generation_service.py`,
`infrastructure/agents/engineer_agent.py`

`PromptBuilder` حالا دو حالت دارد. وقتی context حاوی `codegen_target_file`
باشد، به‌جای دیوار متن، یک پرامپت متمرکز می‌سازد:

```
Write ONE Python file: agent_role.py

FULL PATH:
src/agentplatform/domain/agents/agent_role.py

WHAT agent_role.py MUST DO (this and nothing else):
Agent identity and role enumeration (Phase 2)

LAYER: DOMAIN (innermost).
- Pure Python. No I/O, no HTTP, no filesystem, no database.
- MUST NOT import from application/ or infrastructure/.
- Entities and value objects MUST be @dataclass(frozen=True).
...
```

سه تغییر کلیدی:

۱. **`task_description` کامل حذف شد** — همان چیزی که همه‌ی پرامپت‌ها را شبیه
   هم می‌کرد. `metadata` و `memory_context` هم به ۶۰۰ کاراکتر بریده می‌شوند.
۲. **قوانین مخصوص لایه** — دامنه/اپلیکیشن/زیرساخت/نقطه‌ورود/تست هرکدام
   قوانین خودشان را می‌گیرند. این هم پرامپت‌ها را متمایز می‌کند و هم دقیقاً
   همان چیزهایی را می‌خواهد که در اجرای قبلی غایب بودند (`frozen=True`،
   type annotation، docstring).
۳. **لیست فایل‌های همسایه** — مدل می‌داند چه ماژول‌هایی وجود دارند تا از
   آن‌ها import کند نه اینکه دوباره بنویسدشان.

**اندازه‌گیری:**

| | قبل | بعد |
|---|---|---|
| شباهت دو پرامپت متفاوت | ۹۶.۴٪ | **۸۲.۱٪** |
| ۲۵۰ کاراکتر اول | یکسان | **کاملاً متفاوت** |

آن ۸۲٪ باقی‌مانده عمدی است: بلوک `HARD REQUIREMENTS` مشترک است چون همان
قوانینی است که در هر فایل می‌خواهیم. چیزی که اهمیت دارد این است که **ابتدای
پرامپت** — جایی که مدل بیشترین وزن را می‌دهد — حالا مخصوص همان فایل است.

---

## اصلاح ۲ — چک smoke-run (باگ L)

**فایل:** `application/quality_gate/validators.py` (کلاس `SmokeRunValidator`)

نقاط ورود (`run.py`, `main.py`, `__main__.py`) واقعاً **اجرا** می‌شوند، در
زیرفرایند، با timeout. خروج غیرصفر یا هر استثنای گرفته‌نشده = FAIL.

یک نکته‌ی فنی: `runpy.run_path` نمی‌تواند importهای نسبی داخل package را
resolve کند و خطای گمراه‌کننده‌ی `ImportError: attempted relative import`
می‌داد. حالا اگر فایل `__main__.py` داخل package باشد، با
`runpy.run_module` اجرا می‌شود تا رفتار واقعی `python -m package` سنجیده شود.

**نتیجه روی کد اجرای ۲:**

```
قبل:  [DETERMINISTIC GATE] GREEN (ALL EXECUTED CHECKS PASSED) | executed=4/7
بعد:  [DETERMINISTIC GATE] FAILED | executed=5/8 | failed=smoke_run
      [FAIL] smoke_run: src/agentplatform/__main__.py:
             TypeError: 'dict' object is not callable
             at src/agentplatform/application/stateless_application_service.py:6
```

GREEN دروغین با فایل و شماره‌ی خط دقیق جایگزین شد.

---

## اصلاح ۳ — کش پرامپت (باگ N)

**فایل:** `infrastructure/llm/ollama_provider.py`

کش `sha256(model + prompt)` در حافظه. فقط تطابق **دقیق** hit می‌شود، پس هرگز
دو درخواست متفاوت با هم قاطی نمی‌شوند. با `SHADBOT_LLM_CACHE=0` غیرفعال
می‌شود. شمارنده‌ی `cache_hits` برای سنجش اضافه شد.

در اجرای قبلی ۸ پاسخ از ۱۱ فراخوانی بایت‌به‌بایت یکسان بودند. با اصلاح ۱
انتظار می‌رود پرامپت‌ها متفاوت شوند و کش کمتر hit بخورد — این دو با هم کار
می‌کنند: اصلاح ۱ تکرار را از بین می‌برد، کش هر تکرار باقی‌مانده را ارزان
می‌کند.

---

## اصلاح ۴ — `run.py` واقعی (باگ O)

**فایل:** `infrastructure/agents/engineer_agent.py`

قالب قدیمی «operational» چاپ می‌کرد بدون اینکه چیزی import کند. قالب جدید با
`pkgutil.walk_packages` تمام ماژول‌های تولیدشده را import می‌کند و در صورت
شکست `return 1` می‌دهد.

**اثبات عملی:**

```
run.py قدیم روی پروژه‌ی خراب : "operational"  exit=0   ← دروغ
run.py جدید روی پروژه‌ی سالم : "OK: 5 module(s) imported"  exit=0
run.py جدید با یک import شکسته: "FAILED: 1 module(s)"  exit=1
```

---

## ۱۵ تست رگرسیون

`tests/agentplatform_tests/quality_gate/test_prompt_focus_and_smoke.py`

| گروه | مهم‌ترین تست |
|---|---|
| باگ M (۶) | `test_codegen_prompts_for_different_files_are_distinct` (شباهت < ۹۰٪) |
| باگ L (۶) | `test_smoke_run_catches_runtime_crash_that_imports_cleanly` |
| باگ N (۲) | `test_llm_cache_returns_identical_response_without_second_call` |
| باگ O (۱) | `test_generated_runner_template_imports_modules` |

تست `test_engineer_agent.py` هم به‌روز شد تا بررسی کند `purpose` واقعاً به
لایه‌ی تولید کد می‌رسد.

---

## گیت کامل — حالا ۸ چک

روی یک پروژه‌ی سالم نمونه:

```
[DETERMINISTIC GATE] executed=7/8 | failed=ruff | skipped=pytest
  [PASS] syntax      | [PASS] imports    | [PASS] smoke_run
  [SKIP] pytest      | [FAIL] ruff       | [PASS] mypy
  [PASS] security    | [PASS] architecture
```

`smoke_run` روی کد سالم PASS و روی کد اجرای ۲ FAIL می‌دهد — یعنی چک واقعاً
تفکیک می‌کند و صرفاً همیشه سبز یا همیشه قرمز نیست.

---

## انتظار از اجرای بعدی

۱. **`[LLM] Response received` با طول‌های متفاوت** — دیگر نباید `1746 chars`
   هشت بار تکرار شود.
۲. **کد با type annotation و docstring و `frozen=True`** — چون حالا در پرامپت
   مخصوص لایه صریحاً خواسته شده.
۳. **`[FAIL] smoke_run` اگر کد کرش کند** — دیگر GREEN دروغین نیست.
۴. **زمان کمتر از ۶۵۹ ثانیه** برای بخش engineer.

اگر باز هم کد ضعیف بود، حالا حداقل گیت **صادقانه** می‌گوید کجا شکسته است.
